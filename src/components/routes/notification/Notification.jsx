import React from 'react';
import Spinner from '../../layouts/spinner/Spinner';

const Notification = () => {
  return (
    <div>
      Notification
      <Spinner />
    </div>
  );
};

export default Notification;
